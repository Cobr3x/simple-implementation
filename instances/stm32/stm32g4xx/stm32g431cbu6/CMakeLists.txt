# Generative AI disclosure: Drafted with Microsoft Copilot; revised with OpenAI Codex (SOL model).

include(${CMAKE_CURRENT_LIST_DIR}/../CMakeLists.txt)
set(DEVICE STM32G431xx)
set(DEVICE_LOWER stm32g431xx)
set(INSTANCE_DIR "${CMAKE_CURRENT_LIST_DIR}")
set(STARTUP_BASENAME startup_stm32g431xx.s)
set(AUTH_LCD OFF)
