# Generative AI disclosure: Drafted with Microsoft Copilot; revised with OpenAI Codex (SOL model).

set(FAMILY g4)
set(AUTH_MCPU cortex-m4)
set(AUTH_FPU fpv4-sp-d16)
set(FAMILY_DIR "${CMAKE_CURRENT_LIST_DIR}")
