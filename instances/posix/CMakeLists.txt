if(BUILD_TESTING)
    add_executable(protocol_tests ${PROJECT_SOURCE_DIR}/tests/protocol_tests.c)
    target_link_libraries(protocol_tests PRIVATE auth_core)
    target_compile_options(protocol_tests PRIVATE -Wall -Wextra -Werror -UNDEBUG)
    add_test(NAME protocol COMMAND protocol_tests)
    add_executable(journal_tests ${PROJECT_SOURCE_DIR}/tests/journal_tests.c)
    target_link_libraries(journal_tests PRIVATE auth_core)
    target_compile_options(journal_tests PRIVATE -Wall -Wextra -Werror -UNDEBUG)
    add_test(NAME journal COMMAND journal_tests)
    find_package(Python3 REQUIRED COMPONENTS Interpreter)
    add_test(NAME provisioning COMMAND ${Python3_EXECUTABLE} ${CMAKE_SOURCE_DIR}/tests/test_provision.py)
endif()
